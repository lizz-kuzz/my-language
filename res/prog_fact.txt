decl main() {
   var number = 0;
   scan(number);

   number = fact();

   print_var(number);
   exit;
   
}

decl fact() {

   var i = number;

   while (i) {
      i -= 1;

      if (i) {
         number = number * i;
      }
   }

   return number;
}